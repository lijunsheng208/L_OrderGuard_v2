# 支付成功但库存未扣减

## 症状

订单状态为 `PAID`，支付状态为 `SUCCESS`，但库存状态为 `NOT_DEDUCTED`。

## 调查顺序

先读取订单、支付和库存快照，再查询 `payment.succeeded` 事件和 Outbox 状态。事件不存在时，检查支付服务日志和 Trace。

## 处置原则

只能在确认没有成功扣减记录、订单未发货且库存足够时执行一次幂等扣减。当前阶段只提供调查建议，修复由后续阶段负责。
